
const state = () => ({
	platform: {}, 
	
})
const getters = {
	
}

// 执行异步操作
const actions = {
	
}

// 执行同步操作更新state
const mutations = {
	
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}